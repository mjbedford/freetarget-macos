# Build native addons against Electron's bundled Node, not system Node.
# This prevents compilation errors when system Node is newer than Electron's Node.
runtime=electron
target=28.1.4
disturl=https://electronjs.org/headers
